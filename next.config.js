/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "ntrepidcorp.com",
      "media.licdn.com",
      "store.storeimages.cdn-apple.com",
    ],
  },
};

module.exports = nextConfig;
